# Researcher Agent

The Researcher agent performs research tasks, compiles evidence, and prepares reports. Research outputs should be stored in `artifacts/deep-research/` with manifests, and references should be recorded in `docs/research/`.