# 05 — Component Contracts

Each component must map to this contract before implementation or Storybook story work.

| component | category | purpose | required props | optional props | states | accessibility | stories required | Playwright relevance | implementation notes | anti-patterns |
|---|---|---|---|---|---|---|---|---|---|---|
| `AppShell` | shell | Root application frame with top bar, sidebar, content, optional inspector. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `TopBar` | shell | Global title, environment/status, safe global actions. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `Sidebar` | navigation | Primary navigation and active screen context. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `CommandBar` | navigation | Keyboard/search command surface for navigation and local actions. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `GovernanceStrip` | governance | Compact display of constraints, locks, and readiness status. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `StaticMockDisclaimer` | governance | Persistent honesty marker for local/mock-only surfaces. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `Card` | layout | Standard panel container. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `SectionHeader` | layout | Section title, subtitle, and safe actions. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `KpiCard` | data-display | Metric summary with label, value, status, and trend. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `StatusBadge` | feedback | Compact semantic status marker. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `RiskBadge` | feedback | Risk severity marker. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `EvidenceCard` | evidence | Evidence item summary with trace relationship. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `DecisionCard` | governance | Decision, rationale, status, and evidence link. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `TaskQueueRow` | data-display | Operational task row. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `ApprovalQueueRow` | governance | Approval queue item with Point-lock state. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `ReviewFindingCard` | evidence | Review finding summary with severity, status, evidence. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `HandoffPacketCard` | governance | Work/handoff packet readiness surface. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `TraceTimeline` | evidence | Chronological trace of artifacts and decisions. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `DataTable` | data-display | Accessible dense tabular data display. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `FilterBar` | forms | Search/filter controls for lists and tables. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `Tabs` | navigation | Related view switcher. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `Drawer` | overlays | Side panel for secondary details. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `Modal` | overlays | Focused dialog for confirmation/details. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `Toast` | feedback | Local feedback announcement. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `EmptyState` | feedback | No-data state. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `ErrorState` | feedback | Recoverable failure state. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `LoadingState` | feedback | Loading/skeleton state. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `WarningState` | feedback | Non-fatal issue state. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `WorkflowCanvas` | workflow | Canvas for context/object map. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `OperatorNodeCard` | workflow | Workflow node/operator card. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `NodePalette` | workflow | Safe node/object palette. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
| `InspectorPanel` | overlays | Details/config/debug side panel. | `id`, `label/title`, content fields relevant to object served | `status`, `actions`, `metadata`, `ariaLabel`, `density` | default, dense, empty, loading, warning, error, disabled, selected where applicable | visible label, keyboard reachable, focus state, semantic role, no color-only meaning | default, dense, empty, warning, error, loading/disabled if applicable | capture if screen-critical or regression-prone | use semantic tokens; local/mock actions only | fake live execution; raw styles; ambiguous labels |
